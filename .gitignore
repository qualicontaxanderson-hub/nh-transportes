**/__pycache__/
*.pyc
*.log
.venv/
venv/
.env
.env.local
.env.*.local

# Scripts locais com credenciais de banco em texto puro – nunca versionar
bakup_railway.bat

# OFX inbox – ignore actual OFX files but keep the folder structure
ofx_inbox/*.ofx
ofx_inbox/*.OFX
ofx_inbox/processados/*.ofx
ofx_inbox/processados/*.OFX

# Saida do build do NH-Robo: 967 arquivos e um zip de 26 MB por compilacao.
# O que se distribui e o ZIP, e ele vai para o Dropbox (/BANCOS/NHROBO), nao
# para o git — repositorio nao e lugar de binario que se regenera com um comando.
agente_nhrobo/build/
_lucro_migrado.html
